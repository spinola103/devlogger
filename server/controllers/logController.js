// server/controllers/logController.js

export const getLogs = (req, res) => {
  res.json({ message: "Get logs works!" });
};

export const createLog = (req, res) => {
  res.json({ message: "Create log works!" });
};
